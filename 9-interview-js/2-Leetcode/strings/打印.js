function printString(str) {
  for (let i = 0; i < str.length; i++) {
    console.log(str[i]); // 打印字符
    // 或者存储到数组
    // result.push(str[i]);
  }
}
// 示例用法
const str = "ABCDEFG";
printString(str);
